import { styled, Button } from '@mui/material';

export const StyledButton = styled(Button)({
  gap: 10,
  paddingTop: 11,
  paddingBottom: 11,
  borderRadius: 10,
  fontWeight: 700,
  fontSize: 14,
  lineHeight: '18px',
  textTransform: 'none',
});
