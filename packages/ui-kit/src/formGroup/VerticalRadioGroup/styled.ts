import { FormLabel, Divider, styled } from '@mui/material';

export const StyledFormLabel = styled(FormLabel)({
  fontWeight: 600,
  fontSize: 14,
  lineHeight: '20px',
  letterSpacing: '0.05em',
  textTransform: 'uppercase',
  color: '#5C6673',
  opacity: 0.5,
});

export const StyledDivider = styled(Divider)({
  borderColor: '#E3EAF3',
});
